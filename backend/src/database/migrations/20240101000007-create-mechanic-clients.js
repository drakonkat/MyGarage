'use strict';
/** @type {import('sequelize-cli').Migration} */
export default {
  async up(queryInterface, Sequelize) {
    // This migration was created in error and is intentionally left empty.
    // The correct logic for creating the clients table is in '20240101000007-create-clients.js'.
  },
  async down(queryInterface, Sequelize) {
    // This migration was created in error and is intentionally left empty.
  }
};
